# danesh — Distilled knowledge

The **pajooheshgar** agent writes here. One research note per project:

```
danesh/<slug>-research.md
```

Each note collects setting, period, dialect, customs, real people, verbatim quotes, visual references, open questions, and a project lexicon — distilled from the files you dropped in `raw/`.

Daastansaraa reads from here before writing scenarios so the work feels lived-in rather than generic. Don't write into `danesh/` by hand — drop sources in `raw/` and run `/tahghigh <slug>`.
